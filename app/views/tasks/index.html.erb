<h1>My Tasks...</h1>

<% @tasks.each do |task| %>
<%# link_to "#{task.title}", "tasks/#{task.id}" %>
<%= link_to "#{task.title}", task_path(task) %>

<%= link_to "Delete",
  task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<% end%>

<h1> <%= link_to "Add Task", tasks_new_path %> </h1>
